Function to signup a new user 
function signup(Username, password)
{
    let User = {
        Username:Username,
        password:password
    };
    users.push(user);
    console.log("User signup successfully!")

}